Echo - вывод строки/строк

Требует как минимум один параметр для работы

Ключи:
	-n, --newline
		Каждая из переданных программе строк выводится на новой строке

	-r, --reverse
		Переданные параметры выводятся в обратном порядке

	-h, --help
		Вывод справочного сообщения

Примеры:

	~ $: echo -h
		Выводит справку

	~ $: echo 123
		Вывод:	123

	~ $: echo "example of string"
		Вывод:	example of string

	~ $: echo -n "first" "second" --reverse
		Вывод:	second
				first

	- $: echo -r 'and second' 'first'
		Вывод:	first and second	